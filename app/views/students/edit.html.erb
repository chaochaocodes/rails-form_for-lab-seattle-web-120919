<h3> Edit Student Name </h3>

<%= form_for(@student) do |f| %>
    <%= f.label "first name:" %>
    <%= f.text_field :first_name %>

    <%= f.label "last name:" %>
    <%= f.text_field :last_name %>
    
    <%= f.submit %>
<% end %>
